/**
 * The service's two fixed paths. This file imports nothing, so build
 * configuration can load it directly as `@strike-desk/shared/paths`.
 */
export const WS_PATH = '/ws';
export const HEALTH_PATH = '/healthz';
